import assert from 'node:assert/strict';import {createServer} from 'vite';const server=await createServer({server:{middlewareMode:true}});
try{const {validateCheckpoint}=await server.ssrLoadModule('/src/backend/checkpoints.ts');const p={id:'game',engine:'webmsx',buildSha256:'program',launchHash:'setup-a'};const c={version:1,project:'game',engine:'webmsx',build:'program',launch:'setup-a',state:{PC:1}};validateCheckpoint(c,p);for(const patch of [{launch:'setup-b'},{launch:undefined},{build:'new-program'},{project:'other'},{engine:'other'},{version:2}])assert.throws(()=>validateCheckpoint({...c,...patch},p));validateCheckpoint({...c,launch:undefined},{...p,launchHash:undefined});console.log('PASS checkpoint compatibility: project, program, engine and complete launch fingerprint; legacy only on legacy setup');}finally{await server.close();}
