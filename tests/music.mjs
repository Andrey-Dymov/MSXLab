import assert from 'node:assert/strict';import {createServer} from 'vite';const server=await createServer({server:{middlewareMode:true}});
try{const {psgTone,noteName,musicNotes,musicBasic}=await server.ssrLoadModule('/src/backend/music.ts');const r=Array(16).fill(0);r[0]=254;r[7]=62;r[8]=15;assert.equal(psgTone(r,0).midi,69);assert.equal(noteName(69),'A4');assert(Math.abs(psgTone(r,0).frequency-440)<1);r[7]=63;assert.equal(psgTone(r,0).midi,null);r[7]=62;r[8]=16;assert(psgTone(r,0).envelope);r[8]=15;const rest=[...r];rest[8]=0;const c={version:1,chip:'AY-3-8910',clock:1789772.5,fps:60,frames:60,project:'test',samples:[{frame:0,registers:r},{frame:30,registers:rest}]};const notes=musicNotes(c).filter(n=>n.channel===0);assert.equal(notes.length,2);assert.equal(notes[0].end,30);assert.equal(notes[1].midi,null);const basic=musicBasic(c,120);assert(basic.includes('O4 A'));assert(basic.includes('R R R R'));assert(basic.split('\n').every(l=>l.length<255));assert.equal(musicNotes({...c,frames:0,samples:[]}).length,0);console.log('PASS PSG pitch/mixer/envelope, sustained events/rests, BASIC pitch and bounded aligned lines');}finally{await server.close();}
