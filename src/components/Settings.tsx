import {t,useLanguage,languages} from '../i18n';
import {AiSettings} from './AiSettings';
import {useDebugger} from '../state/debugger';
export function Settings({close}:{close:()=>void}){const [language,changeLanguage]=useLanguage();const s=useDebugger();return <div className="modal-shade" onClick={close}><section className="settings-dialog" role="dialog" aria-label={t("Workspace settings")} onClick={e=>e.stopPropagation()}><header><h2>{t("Workspace settings")}</h2><button aria-label={t("Close settings")} onClick={close}>×</button></header><label className="language-setting">{t("Language")} <select aria-label={t("Language")} value={language} onChange={e=>changeLanguage(e.target.value as keyof typeof languages)}>{Object.entries(languages).map(([code,name])=><option key={code} value={code} lang={code}>{name}</option>)}</select></label><h3>{t("Current session")}</h3><p>{s.project?.name} · {s.snapshot?.engine||s.project?.engine} · {s.project?.machine}</p><p>{t("settings.theme")}</p><h3>{t("Debugger — experimental")}</h3><p>{t("settings.debugger")}</p><h3>{t("Project and engine")}</h3><p>{t("settings.project")}</p><AiSettings/><h3>{t("External control")}</h3><p>{t("settings.remote")}</p></section></div>;}
